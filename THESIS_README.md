# Thesis README

The authoritative thesis reproducibility guide is now the root `README.md`.

This file is kept as a compatibility pointer because older notes and IDE tabs may still reference `THESIS_README.md`. Please use `README.md` for the current setup, including the corrected fact that the baseline and R16 saved runs use matched PPO hyperparameters.
